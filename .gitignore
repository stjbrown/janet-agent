# OS / editor cruft
.DS_Store
*.swp
*~
.idea/
.vscode/

# node / python build output
node_modules/
__pycache__/
*.pyc
.venv/
venv/
dist/
build/
artifacts/

# env / secrets
.env
.env.*
*.local

# local Claude Code settings (personal permissions, not shared)
.claude/settings.local.json

# kb-ingest scratch (temporary plans and raw-source drop zones)
_ingest_plan.md
**/raw/processed/

# firecrawl scrape scratch (fetched docs / working cache)
.firecrawl/

# synchronized copy from @stjbrown/agent-knowledge-skills (build artifact)
skills/

# janet project-local config (thread scope, skill symlinks)
.agent-knowledge/

# vitest snapshots scratch
*.tsbuildinfo
